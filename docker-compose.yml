services:
  migration:
    image: ghcr.io/the-developer-evolution/eternity:felix
    container_name: eternity-migration
    env_file:
      - .env
    command: prisma migrate deploy
    networks:
      - npm_default

  seed:
    image: ghcr.io/the-developer-evolution/eternity:felix
    container_name: eternity-seed
    env_file:
      - .env
    command: prisma db seed
    depends_on:
      - migration
    networks:
      - npm_default

  web:
    image: ghcr.io/the-developer-evolution/eternity:felix
    container_name: eternity
    expose:
      - 3000
    env_file:
      - .env
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.90'
    depends_on:
      - seed
    networks:
      - npm_default

networks:
  npm_default:
    external: true
